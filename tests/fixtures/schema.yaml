datasets:
  interactions:
    userId: id
    itemId: id
  user:
    userId: id
    age: number
    gender: category
  item:
    itemId: id
    price: number
    color: category
model:
  type: two-tower
